- name: keepalived_vip
  type: string
  configurable: true
- name: keepalived_virtual_router_id
  type: integer
  configurable: true
  default: 240

- name: stacks
  type: collection
  configurable: true
  default:
  - name: cflinuxfs2
    description: Cloud Foundry Linux-based filesystem
  - name: windows2012R2
    description: "Microsoft Windows / .Net 64 bit"
<%lifecycle_bundles.each { |bundle| %>
  - name: <%= bundle %>
    description: <%= bundle %>
<% }%>
  property_blueprints:
  - name: name
    type: string
  - name: description
    type: string
- name: lifecycle_bundle_cflinux2
  type: text
  configurable: true
  default: "buildpack/cflinuxfs2:buildpack_app_lifecycle/buildpack_app_lifecycle.tgz"
- name: lifecycle_bundle_windows2012R2
  type: text
  configurable: true
  default: "buildpack/windows2012R2:windows_app_lifecycle/windows_app_lifecycle.tgz"
- name: lifecycle_bundle_docker
  type: text
  configurable: true
  default: "docker:docker_app_lifecycle/docker_app_lifecycle.tgz"
<%lifecycle_bundles.each { |bundle| %>
- name: lifecycle_bundle_<%=bundle%>
  type: text
  configurable: true
  default: "buildpack/<%=bundle%>:buildpack_app_lifecycle/buildpack_app_lifecycle.tgz"
<% }%>
